<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.BestSellingProductsView">

    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding Products}" Margin="20" HorizontalOptions="Center">
            <CollectionView.Header>
                <Grid ColumnDefinitions="0.5*,*,*,*" Padding="0,0,0,8">
                    <Label Text="#"         FontAttributes="Bold" />
                    <Label Text="Product"   Grid.Column="1" FontAttributes="Bold"/>
                    <Label Text="Verkocht"  Grid.Column="2" FontAttributes="Bold" HorizontalTextAlignment="End"/>
                    <Label Text="Voorraad"  Grid.Column="3" FontAttributes="Bold" HorizontalTextAlignment="End"/>
                </Grid>
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="0.5*,*,*,*" Padding="0,4">
                        <Label Text="{Binding Ranking}"      HorizontalTextAlignment="Center"/>
                        <Label Text="{Binding Name}"         Grid.Column="1"/>
                        <Label Text="{Binding NrOfSells}"    Grid.Column="2" HorizontalTextAlignment="End"/>
                        <Label Text="{Binding Stock}"        Grid.Column="3" HorizontalTextAlignment="End"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>